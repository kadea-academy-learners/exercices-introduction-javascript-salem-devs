// Déclaration des variables
let age = 30;                // numérique
let nom = "sam";           // chaîne de caractères
let estConnecte = true;      // booléen
let valeurNulle = null;      // null
let inconnue;                // indéfinie

// Export de toutes les variables pour que Jest puisse les lire
module.exports = { age, nom, estConnecte, valeurNulle, inconnue };
